{
  "name": "makeitpass-project",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "check": "biome check",
    "format": "biome format --write",
    "fix": "biome lint --fix",
    "test": "turbo test",
    "cs": "changeset",
    "pub": "turbo run build lint test && changeset version && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.3",
    "turbo": "1.13.4",
    "typescript": "^5.3.3",
    "@biomejs/biome": "1.8.3"
  },
  "packageManager": "pnpm@9.4.0",
  "engineStrict": true,
  "engines": {
    "node": ">=20.10.0"
  }
}
